{
  "name": "sipeg-hugo",
  "private": true,
  "scripts": {
    "dev": "hugo server -D",
    "build": "hugo && npx pagefind --site public",
    "postbuild": "echo Build complete",
    "tina:dev": "npx @tinacms/cli dev -c \"hugo server -D\"",
    "tina:build": "npx @tinacms/cli build"
  },
  "devDependencies": {
    "@pagefind/default-ui": "^1.3.0",
    "@tinacms/cli": "^1.12.4",
    "pagefind": "^1.3.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "tinacms": "^2.9.5"
  }
}
