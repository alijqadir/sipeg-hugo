{
  "name": "sipeg-hugo",
  "private": true,
  "scripts": {
    "dev": "hugo server -D",
    "build": "hugo && npx pagefind --site public",
    "postbuild": "echo Build complete"
  },
  "devDependencies": {
    "@pagefind/default-ui": "^1.3.0",
    "pagefind": "^1.3.0"
  }
}
